<?xml version="1.0" encoding="utf-8"?>
<!-- Flutter splash screen background.
     A plain white layer-list is Flutter's standard default.
     You can replace @android:color/white with a custom color or
     add a centered splash image as a second <item> if desired. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white" />
</layer-list>
