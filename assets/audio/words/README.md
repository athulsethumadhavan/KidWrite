# Spoken words (optional)

`assets/audio/words/<character id>.mp3` is played instead of the text-to-speech
voice for that letter's reward word — same id scheme as `assets/words/`.

Without a file here the app speaks the word live (English in en-US, Malayalam in
ml-IN), so this folder is only needed if a recorded voice is preferred.
